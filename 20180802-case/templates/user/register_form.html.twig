{{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.rawPassword.first) }}
    {{ form_row(form.rawPassword.second) }}
    <button class="btn">{{ button_label|default('Register') }}</button>
{{ form_end(form) }}